import { Icon } from "@ui-kitten/components";
import React from "react";

export const HomeIcon = (style) => <Icon {...style} name="home" />;
export const BrowserIcon = (style) => <Icon {...style} name="browser" />;
